# Utopia Studio Onboarding Agent — Environment Variables
# Copy this to .env and fill in your values

# Required for Claude enrichment (optional — agent runs without it)
ANTHROPIC_API_KEY=sk-ant-...

# Required for Slack message sending (optional — agent runs without it)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
